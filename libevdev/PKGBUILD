
pkgname=libevdev
pkgver=1.8.0
pkgrel=2
pkgdesc="Library for handling evdev kernel devices"
arch=('x86_64')
url="https://www.freedesktop.org/wiki/Software/libevdev/"
license=('custom:X11')
depends=('glibc')
makedepends=('python3' 'doxygen' 'linux-headers') 
source=("https://freedesktop.org/software/libevdev/${pkgname}-${pkgver}.tar.xz")
sha256sums=('20d3cae4efd277f485abdf8f2a7c46588e539998b5a08c2c4d368218379d4211')

build() {
   cd ${pkgname}-${pkgver}

   ./configure --prefix=/usr --disable-static
   make
}

package() {
   cd ${pkgname}-${pkgver}
   make DESTDIR=$pkgdir install

   install -Dm644 COPYING ${pkgdir}/usr/share/licenses/${pkgname}/COPYING
}
