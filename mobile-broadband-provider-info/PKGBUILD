
pkgname=mobile-broadband-provider-info
pkgver=20210805
pkgrel=1
pkgdesc="Mobile broadband settings for various service providers"
arch=('x86_64')
url="https://gitlab.gnome.org/GNOME/mobile-broadband-provider-info/"
license=('CC-PD')
source=("https://download.gnome.org/sources/mobile-broadband-provider-info/${pkgver}/${pkgname}-${pkgver}.tar.xz")
sha256sums=('6bf8a1558ea5541afbc6f7b4415e74cc9f5aa9829b134ecab3ef1c87412568bc')

build() {
  cd ${pkgname}-${pkgver}

  ./configure --prefix=/usr
  make
}

package() {
  cd ${pkgname}-${pkgver}
  
  make DESTDIR=${pkgdir} install
}
