
pkgname=dconf
pkgver=0.30.1
_pkgver=0.30
pkgrel=2
pkgdesc="A low-level configuration system."
arch=('x86_64')
url="https://wiki.gnome.org/Projects/dconf"
license=('LGPL2.1')
depends=('glib2')
makedepends=('vala' 'gobject-introspection' 'python2' 'intltool' 'docbook-xsl' 'python3' 'meson' 'ninja')
source=("https://download.gnome.org/sources/dconf/${_pkgver}/${pkgname}-${pkgver}.tar.xz")
sha256sums=('549a3a7cc3881318107dc48a7b02ee8f88c9127acaf2d47f7724f78a8f6d02b7')

build() {
  mkdir -p build
  cd build
  
  meson ../${pkgname}-${pkgver} \
    --prefix=/usr \
    --buildtype=release \
    --libexecdir=/usr/lib/dconf
  ninja
}

package() {
  cd build
  
  DESTDIR=${pkgdir} ninja install
}
