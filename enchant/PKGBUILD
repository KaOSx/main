
pkgname=enchant
pkgver=2.2.6
pkgrel=1
epoch=2
pkgdesc="A wrapper library for generic spell checking"
arch=('x86_64')
url="https://abiword.github.io/enchant/"
license=('LGPL')
depends=('aspell' 'dbus-glib' 'hunspell' 'hspell')
options=('!libtool')
#source=("http://www.abisource.com/downloads/enchant/${pkgver}/${pkgname}-${pkgver}.tar.gz")
source=("https://github.com/AbiWord/enchant/releases/download/v${pkgver}/${pkgname}-${pkgver}.tar.gz")
md5sums=('4d12e13e69b8c00c15153992de4fe989')

build() {
  cd ${pkgname}-${pkgver}
  
  ./configure --prefix=/usr \
    --disable-static \
    --enable-relocatable \
    --with-zemberek
  make 
}

package() {
  cd ${pkgname}-${pkgver}
  
  make DESTDIR=${pkgdir} install 
}
