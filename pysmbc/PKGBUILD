
pkgname=pysmbc
pkgver=1.0.16
pkgrel=1
pkgdesc="Python libsmbclient bindings"
arch=('x86_64')
url="https://github.com/hamano/pysmbc"
license=('GPL')
depends=('python3' 'samba')
makedepends=('python3-setuptools')
source=("https://pypi.io/packages/source/p/${pkgname}/${pkgname}-${pkgver}.tar.bz2")
sha256sums=('62199b5cca02c05d5f3b9edbc9a864fb8a2cbe47a465c0b9461642eb3b6f5aca')

build(){
  cd ${pkgname}-${pkgver}
  
  python3 setup.py build
}

package() {
  cd ${pkgname}-${pkgver}
  
  python3 setup.py install --root=${pkgdir} --optimize=1
}
