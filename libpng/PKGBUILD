
pkgname=libpng
pkgver=1.6.30
_apngver=1.6.30
pkgrel=1
pkgdesc="A collection of routines used to create PNG format graphics files"
arch=('x86_64')
url="http://www.libpng.org/pub/png/libpng.html"
license=('custom')
depends=('zlib' 'sh')
options=('!libtool')
source=("https://downloads.sourceforge.net/sourceforge/libpng/${pkgname}-${pkgver}.tar.xz"
        "https://downloads.sourceforge.net/sourceforge/apng/libpng-${_apngver}-apng.patch.gz")
md5sums=('88a729d47eaf880d61a0ef90f4bd5e8f'
         '5bc11b672ed8b27db3f36dafce603453')

build() {
  cd ${pkgname}-${pkgver}

  # http://sourceforge.net/projects/libpng-apng/
  patch -p0 -i ${srcdir}/libpng-${_apngver}-apng.patch

  ./configure --prefix=/usr --disable-static
  make
}

package() {
  cd ${pkgname}-${pkgver}
  make DESTDIR=${pkgdir} install

  cd contrib/pngminus
  make PNGLIB="-L${pkgdir}/usr/lib -lpng" -f makefile.std png2pnm pnm2png
  install -m755 png2pnm pnm2png ${pkgdir}/usr/bin/
  install -D -m644 ../../LICENSE ${pkgdir}/usr/share/licenses/$pkgname/LICENSE
}
