
pkgname=proj
pkgver=5.0.1
pkgrel=1
pkgdesc="Cartographic Projections library"
arch=('x86_64')
url="https://proj4.org/"
license=('MIT')
options=('!libtool')
source=("https://download.osgeo.org/proj/${pkgname}-${pkgver}.tar.gz"
        "https://download.osgeo.org/proj/${pkgname}-datumgrid-1.7.zip")
md5sums=('15c8d7d6a8cb945c7878d0ff322a232c'
         '156a20036ab6ce0e62fb1c3f1cb8d946')

build() {
  bsdtar -xzvf ${srcdir}/${pkgname}-datumgrid-1.7.zip -C ${srcdir}/proj-${pkgver}/nad
  cd ${srcdir}/proj-${pkgver}

  ./configure --prefix=/usr
  make
}

package() {
  cd ${pkgname}-${pkgver}
  make DESTDIR=${pkgdir} install

  install -D COPYING ${pkgdir}/usr/share/licenses/${pkgname}/LICENSE
}
