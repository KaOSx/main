
pkgname=python2-cairo
_pkgname=pycairo
pkgver=1.16.2
pkgrel=1
pkgdesc="Python bindings for the cairo graphics library"
url="https://www.cairographics.org/pycairo"
arch=('x86_64')
license=('LGPL' 'MPL')
depends=('python2' 'cairo')
options=('!libtool')
source=("https://github.com/pygobject/pycairo/releases/download/v${pkgver}/${_pkgname}-${pkgver}.tar.gz")
sha256sums=('49a3cf8737c009852e97289d43e952bf228d8df53a7ddb840d4deeb4d0cc1ea7')

build() {
  cd ${_pkgname}-${pkgver}
  
  python2 setup.py build
}

package() {
  cd ${_pkgname}-${pkgver}
    
  python2 setup.py install --prefix=/usr --root=${pkgdir} --optimize=1 --skip-build
}
