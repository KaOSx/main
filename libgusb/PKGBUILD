
pkgname=libgusb
pkgver=0.4.4
pkgrel=1
pkgdesc="GLib wrapper around libusb1"
arch=('x86_64')
url="https://github.com/hughsie/libgusb"
license=('LGPL2.1')
depends=('systemd' 'libusb' 'json-glib')
makedepends=('gobject-introspection' 'vala' 'meson' 'ninja' 'python3-setuptools')
source=("https://github.com/hughsie/libgusb/releases/download/${pkgver}/${pkgname}-${pkgver}.tar.xz")
options=('!libtool')
sha256sums=('f4123a5323812dcc30bc5b711133890e15b81ef6b2701e8801193cd012ee2fa5')

build() {
  mkdir -p build
  cd build

  meson setup ../${pkgname}-${pkgver} \
    --prefix=/usr \
    --buildtype=release \
    -Ddocs=false
  ninja
}

package() {
  cd build

  DESTDIR=${pkgdir} ninja install
}

