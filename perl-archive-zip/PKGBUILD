
pkgname=perl-archive-zip
_pkgname=Archive-Zip
pkgver=1.67
pkgrel=2
pkgdesc="Provide a perl interface to ZIP archive files"
arch=('x86_64')
license=('GPL' 'PerlArtistic')
url="https://search.cpan.org/dist/Archive-Zip"
depends=('perl')
options=('!emptydirs')
source=("https://search.cpan.org/CPAN/authors/id/P/PH/PHRED/${_pkgname}-${pkgver}.tar.gz")
md5sums=('f18c6550236fa7b337086bee0a373ed9')

build() {
  cd ${_pkgname}-${pkgver}

  PERL_MM_USE_DEFAULT=1 perl Makefile.PL INSTALLDIRS=vendor
  make
}

package() {
  cd ${_pkgname}-${pkgver}
  
  make install DESTDIR=${pkgdir}
}
