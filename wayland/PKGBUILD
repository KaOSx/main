
pkgname=wayland
pkgver=1.17.0
pkgrel=2
pkgdesc="Wayland the modern display server."
arch=('x86_64')
url="https://wayland.freedesktop.org"
license=('MIT')
depends=('libffi' 'expat')
#makedepends=('doxygen' 'xmlto') requires graphviz for documentation
options=('!makeflags' '!libtool' 'debug')
source=("https://wayland.freedesktop.org/releases/${pkgname}-${pkgver}.tar.xz")
sha256sums=('72aa11b8ac6e22f4777302c9251e8fec7655dc22f9d94ee676c6b276f95f91a4')

build() {
	cd ${pkgname}-${pkgver}

	./configure --prefix=/usr \
        --disable-static \
        --disable-documentation
	make 
}

package() {
     cd ${pkgname}-${pkgver}
     make DESTDIR=${pkgdir} install
     
     install -Dm 644 COPYING ${pkgdir}/usr/share/licenses/${pkgname}/COPYING
}
