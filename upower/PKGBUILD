
pkgname=upower
pkgver=0.99.10
pkgrel=1
pkgdesc="Abstraction for enumerating power devices, listening to device events and querying history and statistics"
arch=('x86_64')
url="https://upower.freedesktop.org"
license=('GPL')
depends=('systemd' 'libgudev' 'libusb' 'polkit' 'dbus-glib' 'libimobiledevice')
makedepends=('pkg-config' 'intltool' 'docbook-xsl')
options=('!libtool')
install=upower.install
source=("https://gitlab.freedesktop.org/upower/upower/uploads/c438511024b9bc5a904f8775cfc8e4c4/${pkgname}-${pkgver}.tar.xz"
        '0.99.8.patch')
sha1sums=('9b452bc3d85d749d63644c51cc2fa0465890f659'
          '0ead0e6f972cf03e532d10d267e210f6296d1632')

build() {
  cd ${pkgname}-${pkgver}
  #patch -p1 -i ${srcdir}/0.99.8.patch

  ./configure --prefix=/usr \
      --sysconfdir=/etc \
      --localstatedir=/var \
      --libexecdir=/usr/lib/upower \
      --with-systemdsystemunitdir=/usr/lib/systemd/system \
      --disable-static \
      --enable-deprecated
  make
}

package() {
  cd ${pkgname}-${pkgver}
  
  make DESTDIR=${pkgdir} install
}
